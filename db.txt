select studentNum
from exempt

1
2
3



CREATE TABLE self_report (
    studentNum int,
   PRIMARY KEY(studentNum)
);
INSERT INTO self_report (studentNum)
Values(12)






CREATE TABLE close_contact (
    studentNum int,
   PRIMARY KEY(studentNum)
);
INSERT INTO close_contact (studentNum)
Values(11)
